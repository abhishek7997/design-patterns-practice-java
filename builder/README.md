# Builder Design pattern

It is a creational design pattern used to construct objects without having to write different overloaded constructors, helps in avoiding telescoping constructor problem.

Especially when there are quite a lot of optional fields, and where some validations are required.
